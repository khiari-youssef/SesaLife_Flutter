import 'package:core/exports.dart';

class StudentSubscriptionRecord extends StatelessWidget {
  final StudentSubscriptionRecord record;
  const StudentSubscriptionRecord({super.key, required this.record});

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: SizedBox(),
    );
  }
}
